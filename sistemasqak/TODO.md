1. gradle init
2. import > existing gradle project > importa il progetto
3. in alto su filter 
4. aggiungi la "natura" java
5. new -> source folder chiamata src

poi per importare le dipendenze

sudo cp ~/uni/mag-iss/issLab2026/plugins/*.jar /usr/lib/eclipse/dropins/

sudo chmod 644 /usr/lib/eclipse/dropins/*.jar

eclipse -clean &

nella src crea un nuovo file, chiamalo .qak
